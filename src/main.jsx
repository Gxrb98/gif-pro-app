import React from 'react';
import ReactDOM from 'react-dom/client';
import GiftProAPP from './GiftProApp.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GiftProAPP />
  </React.StrictMode>,
)
